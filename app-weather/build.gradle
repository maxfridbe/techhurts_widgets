plugins {
    id 'com.android.application'
}

// Thin wrapper: everything lives in the :app library, which the widget
// pack also includes. This module only gives it an identity and signs it.
android {
    namespace 'com.techhurts.weatherwidget.app'
    compileSdk 34

    defaultConfig {
        applicationId "com.techhurts.weatherwidget"
        minSdk 21
        targetSdk 34
        versionCode((project.findProperty('appVersionCode') ?: '1').toInteger())
        versionName project.findProperty('appVersionName') ?: '0.0000.000-dev'
    }

    signingConfigs {
        debug {
            storeFile file("../debug.keystore")
            storePassword "android"
            keyAlias "androiddebugkey"
            keyPassword "android"
        }
    }
    buildTypes {
        debug { signingConfig signingConfigs.debug }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation project(':app')
}
